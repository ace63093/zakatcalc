---
name: zakat-legacy-reader
description: Safely reference ../legacy-zakat to extract zakat rules, nisab logic, terminology, and UI wording without copying structure. Use when asked to incorporate legacy logic or port behavior.
allowed-tools: Read, Glob, Grep, Bash
---
Rules:
- ../legacy-zakat is READ-ONLY.
- Extract behavior/terminology only; never copy structure.
- Summarize: carry-forward behaviors, avoid list, open questions (only if blocking).
