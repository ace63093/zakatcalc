# ============================================
# R2 Configuration (Cloudflare S3-compatible)
# ============================================
# Used for caching pricing snapshots across deployments
# When enabled, pricing data is shared via R2 to avoid
# repeatedly fetching from upstream providers.

R2_ENABLED=1
R2_BUCKET=your-bucket-name
R2_ENDPOINT_URL=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_PREFIX=zakat-app/pricing/

# ============================================
# Pricing Configuration
# ============================================
PRICING_ALLOW_NETWORK=1
